<x-app-layout>
    {{-- This outer div sets up the main layout: a column layout on small screens --}}
    {{-- and a row layout (sidebar + content) on medium screens and larger. --}}
    {{-- `min-vh-100` ensures it takes at least the full viewport height. --}}
    <div class="d-flex flex-column flex-md-row min-vh-100">

        {{-- Desktop Sidebar --}}
        {{-- This sidebar is hidden by default (`d-none`) and only becomes visible --}}
        {{-- on medium (`md`) screens and larger (`d-md-block`). --}}
        {{-- It has a fixed width and a custom background color. --}}
        <div class="p-3 d-none d-md-block" style="width: 250px; min-width: 250px; background-color: #A6A6A6;">
            <h4 class="fw-bold text-white text-center">Boat Owner</h4>
            <ul class="nav flex-column mt-3">
                {{-- Navigation links for the desktop sidebar --}}
                <li class="nav-item mt-2">
                    {{-- The 'active' class visually highlights the current page. --}}
                    <a href="#" class="nav-link text-white bg-secondary rounded p-2 text-center active">Boat Information</a>
                </li>
                <li class="nav-item mt-2">
                    <a href="#" class="nav-link text-white bg-secondary rounded p-2 text-center">Dashboard</a>
                </li>
                <li class="nav-item mt-2">
                    <a href="#" class="nav-link text-white bg-secondary rounded p-2 text-center">Verified</a>
                </li>
                <li class="nav-item mt-2">
                    <a href="#" class="nav-link text-white bg-secondary rounded p-2 text-center">Notification</a>
                </li>
            </ul>
        </div>

        {{-- Mobile Offcanvas Toggle Button --}}
        {{-- This button is only visible on screens smaller than medium (`d-md-none`). --}}
        {{-- It's used to open the offcanvas sidebar for navigation on mobile devices. --}}
        <div class="d-md-none bg-light border-bottom p-2">
            <button class="btn btn-outline-secondary" type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileSidebar" aria-controls="mobileSidebar">
                &#9776; {{-- Hamburger icon for the mobile menu --}}
            </button>
        </div>

        {{-- Mobile Offcanvas Sidebar --}}
        {{-- This is Bootstrap's Offcanvas component, acting as the mobile sidebar. --}}
        {{-- It slides in from the start (left) and has the same styling as the desktop sidebar. --}}
        <div class="offcanvas offcanvas-start" tabindex="-1" id="mobileSidebar" aria-labelledby="mobileSidebarLabel" style="background-color: #A6A6A6; color: white;">
            <div class="offcanvas-header">
                <h5 class="offcanvas-title fw-bold text-white" id="mobileSidebarLabel">Boat Owner</h5>
                {{-- Close button for the offcanvas menu --}}
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body">
                <ul class="nav flex-column">
                    {{-- Navigation links for the mobile offcanvas sidebar (same as desktop) --}}
                    <li class="nav-item mt-2">
                        <a href="#" class="nav-link text-white bg-secondary rounded p-2 text-center active">Boat Information</a>
                    </li>
                    <li class="nav-item mt-2">
                        <a href="#" class="nav-link text-white bg-secondary rounded p-2 text-center">Dashboard</a>
                    </li>
                    <li class="nav-item mt-2">
                        <a href="#" class="nav-link text-white bg-secondary rounded p-2 text-center">Verified</a>
                    </li>
                    <li class="nav-item mt-2">
                        <a href="#" class="nav-link text-white bg-secondary rounded p-2 text-center">Notification</a>
                    </li>
                </ul>
            </div>
        </div>

       
    </div>
</x-app-layout>
